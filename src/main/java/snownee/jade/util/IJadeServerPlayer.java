package snownee.jade.util;

public interface IJadeServerPlayer {

	String jade$getClientVersion();

	void jade$setClientVersion(String version);

	boolean jade$isConnected();

	void jade$setConnected(boolean is);
}
